vector<int> kmp(string &s){
	int n = sz(s);
	vector<int> p(n);
	p[0] = 0;
	for(int i = 1; i < n; ++i){
		p[i] = p[i-1];
		while(p[i] and s[p[i]] != s[i]) p[i] = p[p[i]-1];
		if(s[p[i]] == s[i]) p[i]++;
	}
	return p;
}
